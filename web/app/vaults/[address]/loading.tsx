import { VaultSkeleton } from "@/components/vaults/vault-skeleton";

/** Shown the moment a coin is opened, until its page is ready. */
export default function Loading() {
  return <VaultSkeleton />;
}
